# Task 16.1 completion — production documentation and governance

Status: completed and pushed to `origin/main` through
`143aece1a239689482d1620fa5502fe2797cc839`.

## Production and architecture documentation

The README and production guide now lead with adaptive, target-preserving,
full-frame geography-conditioned dynamic pooling. Family and geography are
evidence and scheduling inputs rather than identity gates. Compatible Flickr
and reference embeddings are immutable and reusable across pool changes. The
family/genus cascade, 0.90 shortcut, crops and bucket paths remain documented
only as explicit compatibility or diagnostic behavior.

The accepted ADR now distinguishes completed software/fixture implementation
from pending live empirical selection. The new schema catalog maps canonical
Parquet/JSON artifacts to their grains, schema versions, producers, validators,
semantic identities and scientific authority. It also records exact TaxaLens
and ButterflyLens consumer pins and their fail-closed handoff boundaries.

## Human decision and resumable agent state

The human governance record accepts the dynamic-pooling architecture while
preserving the frozen Phase 15 selection outcome: `insufficient_evidence`.
Zero of 24 variants are eligible; candidate strategy, pool variant, fusion
method and selection-evidence fingerprints remain unset; runtime settings are
unchanged.

The decision binds the exact settings, decision, selection-table and integrated
report fingerprints. It does not convert a fixture projection into a selected
default. A later selection still requires the complete eligible live/review,
subgroup, reviewed-precision, comparable-computation and MPS evidence gates.

The root and topic AGENTS documents now point future work to Task 16.2 final
release verification. Their pack manifest is synchronized and tested against
the byte count, line count and SHA-256 of every listed instruction file.

## Verification and provenance

- Task documentation/ADR/governance gate: 35 passed in 0.46 seconds.
- Subtask focused gates: 35 production-documentation, 14 architecture/schema,
  and 22 governance/agent-handoff checks passed.
- Changed-file Ruff, JSON parsing, pack-manifest integrity, and
  `git diff --check`: passed.
- Remote `origin/main` resolved exactly to `143aece…` after the implementation
  push.
- GitHits calls: zero, per the user's directive; direct code and architecture
  contribution from GitHits: none.

Task 16.1 documents implemented software and fixture behavior. It does not
select a production default, complete human review, establish statistical
support, prove live biological performance, or authorize occurrence release.
