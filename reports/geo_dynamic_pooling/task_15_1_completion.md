# Task 15.1 completion — bounded dynamic-pooling pilot

Status: completed and pushed to `origin/main` through
`7cad1fe3e3eb7a0b3e650de6885b3a9639e1da1a`.

## Delivered

BioMiner now has an immutable seven-case pilot spanning five Papilionidae taxa,
six located Australian-scope fixture regions, and one explicit no-geography
case. The plan freezes three target-preserving candidate schedules, global-only
and dynamic global/local pool variants, all four provisional raw fusion methods,
durable input identities, acceptance criteria, and scientific authority limits
before evaluating results.

The current execution is deliberately fixture-backed. Historical real-source
inventory remains identified separately; it is not presented as a current
dynamic-pooling execution. Deterministic synthetic 8-dimensional vectors pass
through the production cached-vector scoring boundary without loading BioCLIP,
materializing images, or fitting weights from fixture labels.

## Candidate, score, and reuse evidence

All three schedules preserve the same complete five-taxon union and the target
in all seven cases. Geography-first and parallel-union place the fixture target
first in 7/7 cases; family-first-safe places it first in 1/7 and within the first
three in 7/7. These are structural fixture ranks, not reviewed classification
accuracy, timing evidence, or production-selection evidence.

Fourteen vector work items produce 168 result projections: seven cases by three
schedules, two pool variants, and four fusion methods. Seven query vectors are
consumed fourteen times. The run records 100 pool-matrix references, 35 unique
matrices, and 65 within-batch reuses, with zero encoder invocations and zero
image materializations. Among 72 located global/dynamic comparisons, 36 target
raw scores change but no top candidate changes. All 12 no-geography comparisons
retain exact global-fallback parity.

## Review and authority boundary

The frozen projection produces seven representative fixture work items with
explicit probability-one inclusion and unit weights, plus a separate seven-item
targeted failure-discovery queue. Six targeted rows expose pool disagreement and
one preserves the explicit no-geography condition. Targeted rows have no
inclusion probabilities or estimation weights. No item enters occurrence-release
review.

There are zero reviewer identities, assignments, completed reviews, decisive
reviews, or adjudications. Fixture work contributes zero effective real reviews,
so the preregistered 86-review minimum retains its full shortfall of 86 and the
30-independent-record subgroup floor remains untested. Reviewed precision,
calibration, statistical support, production selection, and occurrence release
are unavailable or blocked.

## Verification and provenance

- Focused bounded-pilot gate: 36 passed in 12.63 seconds.
- Full BioMiner regression: 3,205 passed in 134.18 seconds.
- Changed-file Ruff, formatting, and `git diff --check`: passed.
- Remote `origin/main` resolved to `7cad1fe…` after the implementation push.
- GitHits calls: zero, per the user's directive; direct and architectural
  contribution from GitHits: none.

The pilot establishes a reproducible reporting boundary. It does not establish
live accuracy, calibrated probabilities, a superior strategy, a production
default, completed human review, biological absence from missing geography, or
release-ready occurrence evidence.
